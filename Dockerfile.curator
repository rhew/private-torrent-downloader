FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY curator ./curator

EXPOSE 8787

CMD ["python", "-m", "curator.server", "--config", "curator/server.toml", "--host", "0.0.0.0", "--port", "8787"]
